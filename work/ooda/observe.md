# ORDER//3 OODA — Observe / QA（ACT 09後）

更新: 2026-09-11 22:56 JST

## 判定

**ACT 08の主仮説「火種の罠は、その敵行動を消さず、踏んだ移動イベントの残り歩数だけを失わせることで、縫い留め・退避・護衛とは異なる位置回答になる」は成立した。自動テストは全件成功し、通常UIでもNORMAL移動、次ターンFAST移動、隣接／非隣接の両方で予測＝実行＝次ターン盤面が一致した。**

ただし、罠を置く前に敵の中間通過マスを盤面上で読めないため、戦術として使いこなすには推測と一手戻しが必要になる。これは罠の中核判断を阻害するP1の可読性課題である。加えて、隣接攻撃が残る局面の折りたたみ要約と、選択中の停止文言にはP2の表示課題がある。

この周回で変更したのは`work/ooda/observe.md`だけで、`outputs/order-3`の製品ファイルと`work/smoke-test.js`は変更していない。

## 実行した検証

### 自動検査

- `node --check outputs/order-3/game.js`：成功
- `node --check work/smoke-test.js`：成功
- `node work/smoke-test.js`：成功（`ORDER//3 smoke tests passed`）

ACT 08の追加検査で以下を確認した。

- NORMAL飛びかかりの1歩目／2歩目に火種の罠を置くと、敵は罠マスで停止し、HP8→5、罠消滅、Rootなしになる。
- 停止地点が非隣接なら後続攻撃は不発、上下左右に隣接なら後続攻撃／露出はそのまま解決される。
- 先に解決済みのFAST移動へ、同ターン後発のNORMAL火種の罠は遡及しない。
- 前ターンから存在する火種の罠は、次ターンのFAST移動を停止できる。
- 縫い留めでRootになった敵は移動せず、経路上の火種の罠も残る。
- 移動経路外の罠、すでに隣接して移動しない敵のそばの罠は残る。
- 同一経路上に複数の罠がある人工ケースでは最初の1個だけが発動し、別の後続移動イベントは通常どおり進む。
- 純粋シミュレーションと直接実行で、停止位置、HP、罠消費、Root、後続攻撃、構造化イベントが一致する。
- `火種の罠で残り移動停止`の構造化イベントは移動1回につき1件、戦闘ログも1件だけ生成される。

全スモークテストが通っているため、ACT 01〜07の予測＝実行、速度順、FE式上下左右、移動後位置、敵HP、段階予測、印・帯電・連鎖、選択時限定表示、先行戦闘不能理由にも回帰はない。

### ブラウザ実プレイ

前周回と異なる`127.0.0.1:8771`と複数のcache-bust queryを使い、製品状態やDOMを書き換えず通常操作だけで検証した。主プレイはTURN 01からTURN 05まで進め、別の通常プレイで同一意図に対する4回答とFAST後のNORMAL罠を比較した。ブラウザconsole error / warningは全セッション0件だった。

#### NORMAL移動を途中停止し、非隣接攻撃を失敗させる

TURN 02、追跡獣`[5,5]`のNORMAL「飛びかかり」（対象ヴェイル）に対し、イオナがNORMAL火種の罠を`[4,5]`へ設置した。

| 観察点 | 予測 | 実行後 |
|---|---|---|
| 行動順 | `01 イオナ 火種の罠`→`02 追跡獣 飛びかかり` | 同じ順で解決 |
| 移動 | 追跡獣`[5,5]→[4,5]`で停止 | TURN 03も`[4,5]` |
| HP | 追跡獣8→5 | 5/8 |
| 罠 | `火種の罠1マス消滅` | 盤面から消滅 |
| 後続攻撃 | 停止地点がヴェイルと非隣接なので不発 | `追跡獣の飛びかかりは届かなかった。` |
| 停止ログ | 構造化イベント1件 | `火種の罠で残り移動停止`が1件 |

罠位置は実装を把握した状態では最初の1回で当てられた。停止後の敵HPは戦場と敵予告の両方で`8→5`、次ターンは`5/8`と表示された。

#### 前ターンの罠がFAST移動を止め、隣接攻撃だけ残る

TURN 03にルークを`[2,5]→[1,5]`、イオナを`[3,6]→[2,6]`へFAST移動し、空いた`[2,5]`へNORMAL火種の罠を設置した。追跡獣はSLOW移動で`[4,5]→[3,5]`まで進んだが、このターンは罠経路に入らず、罠はTURN 04へ残った。

TURN 04のFAST「忍び寄る」で追跡獣は`[3,5]→[2,5]`と1歩進んで罠を踏み、残り1歩を失った。予測と実行はいずれも追跡獣HP7→4、罠消滅だった。停止後は対象イオナ`[2,6]`と上下左右に隣接しているため2ダメージが残り、イオナHP1→0になった。TURN 05の盤面は追跡獣`[2,5]`・HP4/8、イオナ0/7で予測どおりだった。

この結果から、罠は敵行動全体や後続効果をキャンセルせず、「現在の移動イベントの残り歩数」だけを止めている。

#### FAST解決後のNORMAL罠は遡及しない

別プレイのTURN 01で、イオナを`[3,6]→[4,6]`へFAST移動した。その後、追跡獣がFAST「忍び寄る」で`[5,3]→[5,5]`へ進み、通過済みの`[5,4]`へイオナがNORMAL火種の罠を設置した。

- 追跡獣はHP8/8のまま`[5,5]`まで移動した。
- `火種の罠で残り移動停止`は予測にも実ログにも出なかった。
- TURN 02開始時も`[5,4]`の罠は残った。

速度順の表示も`イオナ移動 FAST`→`追跡獣 忍び寄る FAST`→他のFAST敵行動→`イオナ 火種の罠 NORMAL`となり、遡及しない理由を時系列で追えた。

#### 火種の罠・縫い留め・退避・護衛の比較

同じ追跡獣のNORMAL「飛びかかり」を基準に比較した。基本局面は追跡獣`[5,5]`、対象ヴェイル`[2,6]`。イオナを`[4,6]`へ退かせて経路を開けると、無対処では追跡獣が`[3,6]`まで到達してヴェイルへ4ダメージを与えた。

| 回答 | 敵の最終位置 | 敵への効果 | 対象への結果 | 位置回答としての違い |
|---|---:|---:|---|---|
| 火種の罠`[4,5]` | `[4,5]` | 3ダメージ、残り移動だけ停止、罠消滅 | 飛びかかり不発 | 敵を途中位置へ誘導して削る |
| 縫い留め | `[5,5]` | 1ダメージ、Root | 飛びかかり不発 | 敵を始点へ固定する |
| ヴェイル退避`[2,6]→[1,6]` | `[3,4]` | なし | 飛びかかり不発 | 標的位置を変えて敵の着地点を変える |
| 盾を固める | `[3,6]` | なし | ルークの装甲5→1で肩代わり | 敵の到達を許し、味方側で被害を受ける |

いずれもACTION ORDERの全ユニット混在タイムラインで、誰が先に動き、最終的にどこへ行き、HP／装甲がどう変わるかを追えた。回答を網羅比較するため同一プレイ内で6回一手戻しを使ったが、各回答を単独で成立させる操作に余分な戻しは不要だった。

縫い留めを既存罠のあるFAST「忍び寄る」に使った予測では、追跡獣は元位置に残りHP7→6、罠も盤上に残った。経路外`[5,6]`へ置いた罠も、追跡獣が`[5,5]→[3,6]`と移動・攻撃した後に残る予測になった。

## 優先度別の観察事項

### P0

なし。

### P1

#### [P1] 罠を確定する前に敵の中間通過マスが盤面へ表示されず、経路を推測する必要がある

- 再現: TURN 02の追跡獣「飛びかかり」で火種の罠を選択し、配置候補を見る。
- 期待: 現在予告されている敵移動の予定経路を、`1→2→3`、矢印、線などで盤面上に表示し、踏まれる候補マスと経路外マスを配置確定前に区別できる。
- 実際: 対象確定前の敵行動は`対象確定後に再予測`となり、中間経路は出ない。確定後もACTION ORDERは始点→終点、盤面は最終位置だけで、通過マスの列は見えない。`[4,5]`なら発動、`[5,6]`なら残ることは、置いて結果を見比べるまで判別できない。
- 影響: 罠の強さが「経路を読む」面白さではなく「内部の経路選択を当てる」試行錯誤へ寄る。特に障害物や同距離経路がある局面で、意図した戦略と結果を対応づけにくい。
- 再現性: 100%（通常UI、複数セッション）。

### P2

#### [P2] 隣接攻撃が残る罠発動では、折りたたみ要約が敵自身への罠ダメージと味方への攻撃を合算する

- 再現: TURN 04、追跡獣が`[3,5]→[2,5]`で罠を踏み、隣接するHP1のイオナへ2ダメージを続ける。
- 期待: 折りたたみ行は、因果順に`火種の罠で残り移動停止 / 追跡獣に3ダメージ / イオナ撃破`のように、罠の自傷側と敵攻撃側を区別する。
- 実際: 先頭要約が`2人に計4ダメージ（1人撃破） / 火種の罠で残り移動停止 / ＋他2種`となり、追跡獣HP7→4とイオナHP1→0を同じ「2人へのダメージ」に合算する。詳細を開けば判別できるが、折りたたみ行だけでは敵が2人を攻撃したようにも読める。
- 再現性: 100%（該当局面）。

#### [P2] 行動詳細を開くと停止文言が同じ画面に2回見える

- 再現: 罠で停止する敵のACTION ORDER行を選択して詳細を開く。
- 期待: `火種の罠で残り移動停止`を現在画面内の主表示1か所にまとめる。
- 実際: 折りたたみ行の要約と、その直下の詳細見出しに同じ文言が同時表示される。基礎データの構造化イベントは1件、実ログも1件であり、二重解決ではない。
- 再現性: 100%（行動詳細表示中）。

## 次の観察候補

1. 火種の罠選択中に、敵ごとの予定経路と踏む順序を盤面へ重ね、障害物・同距離分岐でもプレイヤーが一手戻しなしに置けるか確認する。
2. 罠ダメージと敵の後続攻撃を折りたたみ要約で分離し、停止原因→位置→被害の順で一読できるか確認する。
3. 構造化イベント1件という意味を保ちつつ、行動行と展開詳細で停止文言を重複表示しないレイアウトを確認する。

# USER PLAYTEST / ACT 09 Observe

## 判定

**ユーザー報告「倒した敵がフィールドに残り続ける」は再現した。P1の表示不具合である。** 追跡獣を通常操作だけで撃破すると、HP0になった直後、次ターンの通常盤面、さらに戦闘勝利時まで同じ座標に駒が残った。通常盤面では撃破を示すクラスや文言がなく、`unit enemy`の生存駒と同じ外観構造で`HP 0/8`とだけ表示される。

一方、対象判定・経路・敵予告・勝敗判定はHP0ユニットを除外しており、死亡した敵が実際に行動したり通行を妨害したりするルール破綻はなかった。セルは空きマスとして罠配置・移動先に使えるため、**ルール上は空なのに盤面上は占有されて見える**ことが混乱の中心である。最終予測だけに出る半透明の撃破予定ゴーストは予測情報として成立し得るが、実行完了後の通常盤面に同じ駒を残す理由にはならない。

この周回では`work/ooda/observe.md`だけを更新し、`outputs/order-3/**`と`work/smoke-test.js`は変更していない。

## 再現環境と結果

- 新規localhost: `http://127.0.0.1:8772/?act=9-ko`
- ブラウザ: 通常UI操作のみ。DOMやゲーム状態の書き換えなし。
- 自動検査: `node --check outputs/order-3/game.js`、`node --check work/smoke-test.js`、`node work/smoke-test.js`はすべて成功（`ORDER//3 smoke tests passed`）。
- ブラウザconsole error / warning: 0件。
- 戦闘はTURN 01から自然に進め、TURN 02で追跡獣、TURN 09で城壁兵、TURN 12で詠唱師を撃破して勝利ダイアログまで確認した。

## 主再現手順

1. TURN 01でイオナとヴェイルを前進させ、ヴェイルの狩人の印を追跡獣へ付与する。
2. TURN 02、`[4,5]`へ火種の罠を置く。追跡獣が飛びかかりで罠を踏み、HP8→2で同マスに停止する。
3. イオナの連鎖火花を追跡獣へ指定する。最終予測でHP8→0・撃破を確認し、作戦実行する。
4. 撃破スナップショット、実行完了、TURN 03の通常盤面を順に確認する。
5. TURN 03で火種の罠、連鎖火花、移動命令をそれぞれ選び、死亡した追跡獣のセル`[4,5]`に対する配置・攻撃対象・移動先の扱いを確認する。
6. 戦闘を継続して全敵を撃破し、勝利ダイアログ背後の盤面を確認する。

## 観察点別の期待／実際

| 観察点 | 期待 | 実際 |
|---|---|---|
| 現在盤面（撃破前） | 生存敵を通常表示 | 追跡獣`[4,5]`、HP2/8で正常 |
| カード選択中の命令直前盤面 | その命令直前の生存状態と対象可否を表示 | 連鎖火花選択時は追跡獣HP8→2、合法対象で正常 |
| 最終予測 | 撃破予定を生存駒と明確に区別 | `HP 8→0`、`unit enemy projected-unit predicted-ko`、HPバー0%。半透明・グレースケールで区別あり |
| 撃破解決中 | 撃破した瞬間から盤面駒を除去するか、短い明示的な撃破演出後に除去 | HP0の敵駒がセル子要素として残る。追跡獣では通常の`unit enemy`へ戻る局面も確認 |
| 実行完了／次ターン | HP0敵を盤面から除去 | `[4,5]`に`P / 追跡獣 / HP 0/8`が残留。`dead`、`down`、`predicted-ko`のいずれもない通常`unit enemy` |
| セル占有 | 見た目とルール上の空き／占有が一致 | DOM上は子要素1個で敵駒が見えるが、ルール上は空き。火種の罠を同じ`[4,5]`へ設置できた |
| クリック | 死亡敵を攻撃対象として選べず、空きマス用途は空に見える | セルbuttonは有効。罠配置先としてクリック可能だが、死亡駒と罠`✦`が同居して見える |
| 攻撃射程 | HP0敵を候補から除外 | 連鎖火花では「固有技の対象がありません」となり正常。ただし目の前に敵駒が見えるため案内と矛盾して感じる |
| 移動／経路 | HP0敵を障害物にしない。盤面も空きに見せる | イオナを`[4,5]`へ移動可能で、経路・占有計算は正常。移動後は生存イオナが優先表示され、死亡敵だけ見えなくなる |
| 敵予告 | HP0敵の行動を生成しない | 追跡獣撃破後は城壁兵・詠唱師だけが予告され、正常 |
| ログ | ダメージと戦闘不能を一度ずつ記録 | `追跡獣に3ダメージ。`、`追跡獣が戦闘不能。`を記録し、正常 |
| 勝利端末 | 勝利を明示し、背後の盤面にも生存駒だけを残す | `演習完了。`ダイアログは正常。ただし背後に詠唱師HP0/7、追跡獣HP0/8、味方イオナHP0/7が残る |

DOM詳細では、TURN 03の追跡獣セルは`aria-label="4列 5行"`、`cellClass="cell"`、`childCount=1`、`tokenClass="unit enemy"`、表示値`HP 0/8`だった。撃破状態はaria-labelにも含まれない。勝利時もHP0ユニットのセルbuttonは有効だった。

## 原因候補

- `previewDisplayUnitAt(state, x, y)`が、まずHP>0ユニットを探した後、同じ座標のHP<=0ユニットへ明示的にフォールバックしている。
- `renderUnit()`が`predicted-ko`を付けるのは`projected && unit.hp <= 0`のときだけである。実行完了後の通常描画ではHP0でも通常の`unit enemy`／`unit player`になる。
- 対照的に、`projectedUnitAt()`、`targetCandidatesForState()`、`buildEnemyIntents()`、`battleResult()`は生存ユニットだけを扱う。このため計算は正しいが、表示だけが死亡レコードを盤面へ戻している。

## 優先度別の観察事項

### P0

なし。HP0敵は攻撃対象・経路障害・敵行動・勝敗判定から除外され、進行不能や誤決着は起きなかった。

### P1

#### [P1] 撃破済みの敵が通常駒として盤面に残り、空きマス判定と視覚が矛盾する

- 再現性: 100%（追跡獣の撃破直後、次ターン、勝利時）。同じ描画経路により撃破済み味方でも再現。
- 影響: 生存敵、攻撃可能対象、経路障害のいずれにも見える。罠や味方を重ねられるため、操作結果をバグと誤認しやすい。
- 最小修正候補: 通常盤面を描く`previewDisplayUnitAt()`からHP<=0へのフォールバックを外し、HP>0だけを返す。死亡レコード自体はログや遺志カード用にstateへ保持してよい。
- 予測表示の扱い: 最終予測で撃破予定を残す場合だけ、明示的な予測コンテキストからゴーストを描く。`predicted-ko`に加えて「撃破予定」表示、×印、`aria-label`への撃破予定追加を行い、通常盤面や勝利端末へ漏らさない。

#### [P1] 戦術判断の主要情報が右・中央・下へ分散し、敵意図から行動順への視線移動が大きい

- 再現: 1280×720の通常表示で、中央の戦場、右の敵予告、戦場下のACTION ORDER／カードを見比べる。
- 実際: 上段は`minmax(560px, 1fr) + 330px`の戦場／右サイド、命令部は別セクションとしてその下にある。初期表示ではACTION ORDERの冒頭までしか見えず、手札・キューはスクロール下にある。
- 影響: 「敵が何をするか」→「自分が何を出すか」→「混在順でどう解決するか」を一つの視野で比較しづらい。実行順の根幹である「左から順に実行」も11pxで、ACTION ORDER見出しより視覚的に弱い。
- 改善候補: 敵意図の直下または戦場と命令の間に、常時見える混在タイムラインを固定する。`左から順に実行`を大きな主見出し／矢印付きルールとして表示し、敵意図・盤面・カード選択のいずれからも同じタイムラインへ短い視線移動で戻れる配置を検証する。

#### [P1] 敵予告の効果説明が小さく低コントラストで、行動名・HP・速度より目に入りにくい

- 再現: 複数敵の予告カードで、行動名、HP、説明、対象、速度を一読する。
- 実際: 説明本文は10pxのmuted色で、12pxの行動名、赤いHP、速度バッジより明確に弱い。`災印を刻む`と`災印起爆`のように効果理解が対処へ直結する行動でも同じ階層である。
- 改善候補: ダメージ、移動、状態、解除方法を短い効果要約として見出し直下へ昇格し、数値と対処キーワードを強調する。説明文を単に大きくするだけでなく、`4ダメージ`、`次ターン起爆`、`柄打ちで解除`を走査可能な単位に分ける。

### P2

#### [P2] 結界などのバフが盤面で一目判別できない

- 再現: 無効印／遺志：残響で結界を付与し、戦場駒とSQUAD STATUSを見る。
- 実際: 戦場では右上の9pxバッジ`◉`、部隊状態では`結界`という文字が付くが、アイコンの意味を示す凡例、色、持続／消費条件の短い表示がない。
- 改善候補: 結界を固有色の輪郭・盾アイコン・短いラベルで統一し、ホバー／フォーカス時に「次の状態異常か地形ダメージを無効」を表示する。装甲、露出、標的、帯電との形・色の衝突も確認する。

#### [P2] カード種別を示すアイコンがなく、罠・攻撃・印・盾を本文で読まないと分類できない

- 再現: 5枚の手札から火種の罠、速射／連鎖火花、狩人の印、盾を固める／無効印を探す。
- 実際: カードは使用者色、名称、本文、ALT移動、速度を表示するが、効果カテゴリの視覚記号はない。
- 改善候補: 罠、攻撃、印、盾に小さく一貫したカテゴリ記号を付ける。複合カードは主カテゴリ1個＋副カテゴリの小バッジまでに抑え、速度バッジや使用者色とは別の軸として読めるか検証する。

## 最小修正後の回帰項目

1. 実行前の現在盤面、各解決スナップショット、実行完了、次ターン、勝利／敗北端末で、HP0ユニットが通常駒としてセル子要素に残らない。
2. 最終予測で撃破予定を表示する場合は`HP n→0`と撃破予定スタイル／ariaが出て、予測を閉じると消える。
3. HP0敵の元セルは見た目にも空きで、移動先・経路・罠配置先として従来どおり使用できる。罠と死亡駒、生存駒と死亡駒が重なって見えない。
4. HP0敵は攻撃／印の対象、敵予告、ACTION ORDERの実行行から除外され、予定済み行動は`取消：行動者が戦闘不能`になる。
5. 撃破ダメージ、戦闘不能ログ、遺志カード生成は各1回のまま。死亡レコードをstateから削除する修正にしてこれらを壊さない。
6. 最後の敵を倒したとき`演習完了。`が出て、勝利ダイアログ背後の戦場に敵のHP0駒が残らない。
7. 味方HP0にも同じ表示除去を適用し、SQUAD STATUSと遺志カードは従来仕様を維持する。

# ACT 09 QA

更新: 2026-09-11 23:34 JST

## 判定

**PASS。ACT 09の受け入れ条件を満たし、新規のP0／P1／P2不具合は確認されなかった。**

製品コードとテストコードには触れず、本節だけを追記した。

## 自動テスト

- `node --check outputs/order-3/game.js`: PASS
- `node --check work/smoke-test.js`: PASS
- `node work/smoke-test.js`: PASS（`ORDER//3 smoke tests passed`）
- HP0の敵味方が現在盤面、命令途中、最終予測、解決スナップショット、次ターン、勝利／敗北背景のセル子要素に残らないことを確認した。
- 死亡レコード、撃破ログ、遺志カード、部隊欄の0HP表示は保持される一方、攻撃対象、敵意図、占有判定、移動元セル表示からは除外されることを確認した。
- 撃破前スナップショットでは生存駒が見え、致死解決以降は消え、戻る操作で撃破前表示へ復帰すること、取り消しで生存状態へ戻ることを確認した。
- 撃破済みユニットの元セルへ罠設置・生存ユニットの移動ができ、死亡駒との重なりや`origin-cell`の残留がないことを確認した。

## 実ブラウザ確認

キャッシュバスター付きの新規localhost（`http://127.0.0.1:8773/?qa=act09-20260911-2315`）をin-app browserで開き、自然進行を2周実施した。

| 観点 | 結果 |
| --- | --- |
| 現在盤面／命令直前 | 致死前の追跡獣はHP4/8で表示され、攻撃命令の最終予測では盤面から消えた。 |
| 解決スナップショット | 撃破前ステップでは表示、致死ステップ・最終状態では非表示。「戻る」で撃破前表示へ可逆的に戻った。 |
| 取り消し | 致死命令を取り消すと追跡獣が生存状態で復帰し、先行命令だけがキューに残った。 |
| 実行中／次ターン | 致死フレーム以降と次ターンでHP0駒、KO表示、ゴースト表示はなく、撃破済み敵の意図も消えた。 |
| 予定行動の失効 | 撃破済み行動者の後続行動は取消ログになり、戦闘不能・ダメージの履歴は保持された。 |
| 死亡セルと移動 | 追跡獣の元セルへ生存味方を移動でき、移動予定位置には生存味方だけが表示された。取り消すと空セルへ戻った。 |
| 死亡セルと罠 | 同じセルへ火種を置けた。罠だけの表示、生存味方＋罠の表示の双方で死亡駒は再表示されなかった。 |
| 対象・意図・占有 | HP0ユニットは攻撃対象、敵予告、移動障害のいずれにもならなかった。 |
| 味方戦闘不能 | イオナ、ヴェイル、ルークはいずれも盤面から消え、SQUAD STATUSの0HP表示と対応する遺志カードは残った。 |
| 勝利 | TURN 14で`演習完了。`を確認。ダイアログ背後にHP0敵は残らなかった。 |
| 敗北 | TURN 11で`部隊壊滅。`を確認。ダイアログ背後には生存敵だけが残り、HP0味方は表示されなかった。 |
| コンソール | warning／errorともに0件。 |

## レスポンシブ確認

- デスクトップ1280×720で横方向のオーバーフローなし。
- 700×900（`max-width: 720px`適用域）でも横方向のオーバーフローなし。
- 狭幅時も移動可能セル、死亡敵の元セル、罠、生存味方の移動予定位置を識別できた。
- 罠だけのセルには罠記号だけが残り、同セルへ移動予定のときは罠と生存味方だけが重なった。

## ACT 01〜08の軽回帰

混在ACTION ORDER、敵意図、FE準拠の上下左右移動候補、攻撃後移動、HP表示、命令プレビュー、解決履歴、勝敗遷移を実ブラウザとスモークテストで確認し、回帰は見つからなかった。

## 優先度別結果

- P0: なし
- P1: なし
- P2: なし

## 残留リスク

ブラウザ上の内部stateはグローバル公開されていないためDOM・ログ・操作結果で確認し、レコード保持そのものは自動テストで補完した。未使用の`.predicted-ko` CSS定義は残るが、現行JavaScriptはこのクラスやKOマーカーを生成せず、表示上の影響はない。

# ACT 10 QA

更新: 2026-09-12 00:27 JST

## 判定

**FAIL。中央の実行読路、6イベントの情報、通常操作、ACT 01〜09の軽回帰は成立したが、狭幅表示でP1を2件確認した。**

製品ファイル、`work/smoke-test.js`、DECIDE／ORIENTは編集せず、本節だけを追記した。

## 自動テスト

- `node --check outputs/order-3/game.js`: PASS
- `node --check work/smoke-test.js`: PASS
- `node work/smoke-test.js`: PASS（`ORDER//3 smoke tests passed`）

## 実ブラウザ環境

- 新規localhost: `http://127.0.0.1:8784/?qa=act10-20260912-0010`
- ブラウザ: Codex in-app browser
- キャッシュバスター付きの新規タブで開始し、TURN 01〜03を通常操作で進行した。
- console warning／error: 0件

## 1280×720

3枚を登録して敵3＋味方3の6イベントにした状態で、スクロール位置`[0,0]`から測定した。

| 観点 | 実測・結果 |
| --- | --- |
| 文書横overflow | `documentElement clientWidth=1265 / scrollWidth=1265`。なし。 |
| 戦場全体 | `top=137 / bottom=534.94`。全体が初期viewport内。 |
| 主方向見出し | `top=102`、`18px`、`font-weight:900`、文言`左から順に実行 01 → 02 → 03 → …`。 |
| 6イベント | 全カードが`top=147.69 / bottom=325.53`かつ左右もviewport内。ACTION ORDERは`clientWidth=723 / scrollWidth=723`で横overflowなし。 |
| 手札上端 | `#hand top=648.94`。初期viewportと交差。 |
| 目視 | 戦場、方向見出し、6イベント、手札カード上部を同時に識別できた。盤面の6×6は維持。 |

## 実行順と情報の完全性

- 味方命令を0→1→2→3枚と追加し、各段階で`buildResolutionEvents()`由来の実順へ挿入された。
- 6イベントすべてに、番号、ENEMY／ALLY、行動者、行動名、速度、対象、効果または`予測：`結果が固定順で表示された。aria-labelも同じ順序だった。
- カード選択中は`この命令`、`対象：選択待ち`、`aria-pressed=true`、`aria-expanded=true`となり、直前スナップショットから合法対象を表示した。確定後は実行者名・対象・予測結果へ更新された。
- 途中イベントを選ぶと`行動順 03 の直後を予測表示中。`となり、盤面位置と詳細が同じ追跡獣の移動後スナップショットへ変化した。
- `最終予測`ではイベント選択が解除され、位相交換後の位置、ルークの移動予定位置、火種の罠予定位置が盤面へ反映された。
- `一手戻す`で3枚目だけがキューから戻り、イベント数6→5、予定罠が消え、カードが手札へ復帰した。再登録も正常。
- 実行開始直後、先頭イベントだけが`.current`、`aria-current="step"`、`aria-expanded=true`となり、`aria-pressed`は付かなかった。詳細と盤面も同じ実行中スナップショットだった。
- ルークの踏み込み斬りで追跡獣を撃破する列では、後続の追跡獣イベントが`取消：息を整える`かつ`予測：取消：行動者が戦闘不能`となった。最終盤面にHP0駒は残らなかった。

## 敵行動3ターンの確認

| TURN | 行動 | 表示内容と強調 |
| --- | --- | --- |
| 01 | 追跡獣「忍び寄る」 | 対象イオナ、2マス移動、隣接時2ダメージ。`2`、`移動`、`ダメージ`だけが白・900で強調。 |
| 01 | 詠唱師「災印を刻む」 | 4マスの座標、予告座標へ固定、次ターンに同じ座標を起爆。`次ターン`を強調。 |
| 02 | 追跡獣「飛びかかり」 | 対象ヴェイル、3マス突進、隣接時4ダメージ。`3`、`突進`、`4`、`ダメージ`を強調。 |
| 02 | 城壁兵「盾の圧力」 | 対象ルーク、1マス接近、隣接時3ダメージ＋露出。`1`、`接近`、`3`、`ダメージ`、`露出`を強調。 |
| 02 | 詠唱師「災印起爆」 | 対象4マス、全マス4ダメージ、柄打ちで詠唱解除可能。`4`、`ダメージ`、`柄打ち`、`解除`を強調。 |
| 03 | 城壁兵「反撃姿勢」 | 自身が対象、装甲6、次の隣接攻撃へ反撃4。`装甲`、`6`、`4`、`ダメージ`を強調。 |
| 03 | 詠唱師「生命吸収」 | 対象ルーク、味方に2ダメージ、敵側負傷者を2回復。両方の`2`、`ダメージ`、`回復`を強調。 |

基礎効果は既存descriptionの原文だけを表示し、結果は`予測：`接頭辞で分離されていた。別ターンの効果や推測値の混入は見つからなかった。

## 敵予告details

- 初期状態は`open=false`で、`#intent-list`の計算済み表示は`none`。
- 閉じた通常状態では`最も近い味方へ2マス移動`と`予告座標へ災印を固定`はいずれも可視テキスト内に1回だけで、旧予告との重複なし。
- 開くと追跡獣HP8/8、城壁兵HP12/12、詠唱師HP7/7、行動名、対象、速度、原文descriptionを読めた。

## 狭幅・フォーカス

### 700×900

- 文書は`clientWidth=685 / scrollWidth=685`で横overflowなし。
- 戦場は`top=127 / bottom=484`、実行パネルは`top=533`、方向見出しは`top=562`、ACTION ORDERは`top=607.69 / bottom=795.53`。盤面→実行読路→COMMAND HANDの順は維持。
- ACTION ORDERだけが`clientWidth=643 / scrollWidth=934`で横スクロールした。最初は`scrollLeft=0`、6番目へのTab移動後は`scrollLeft=291`で最大値へ到達し、最初／最後のイベントカード全体がコンテナ内かつviewport内に入った。
- ただし6イベント時の`#hand top=924.53`で、手札カード上端は初期900px viewportの外だった。COMMAND HAND見出しだけが画面下端に見える。

### 320×900

- 戦場は`left=21 / right=299 / top=141 / bottom=408`で6×6全体を表示し、盤面→実行読路→手札の順は維持。
- ACTION ORDERは`clientWidth=278 / scrollWidth=934`。Tabで1番目は`scrollLeft=0`、6番目は`scrollLeft=654`（最大656）へ移動し、両カードとも全体が見えた。
- `#hand top=893.92`でカード上端は初期viewportと交差した。カード自体は`clientHeight=178 / scrollHeight=178`で縦方向の内容欠落なし。
- ただし文書は`clientWidth=305 / scrollWidth=321`で16pxのページ横overflowがあり、画面下部にページ全体の横スクロールバーが出た。
- さらに手札も`clientWidth=278 / scrollWidth=324 / overflow-x:auto`で、ACTION ORDER以外の第二の横スクロール領域になった。

### 実ブラウザ200%ズーム

in-app browserへ`Ctrl++`、`Ctrl+=`、`Ctrl+Shift+=`、Ctrl＋テンキー加算を送ったが、CSS viewport、visual viewport、DPRが変化せず、この環境ではブラウザクロームの実ズームを操作できなかった。Chrome／Edgeの別ブラウザ接続も利用不可だった。

代替として1280×720の200%相当である640×360 CSS viewportを実ブラウザへ設定して確認した。この条件では文書`clientWidth=625 / scrollWidth=625`、横スクロールはACTION ORDERだけ（`583 / 934`）、盤面→実行読路→手札のDOM／視覚順を維持し、Tabで1番目`scrollLeft=0`、6番目`351`へ到達して両イベント全体を表示できた。これは実ズームそのものの合格証跡ではないため、修正後QAでユーザー環境またはズーム操作可能なブラウザによる再確認が必要。

## ACT 01〜09の軽回帰

- FE式移動: 移動者ルークの合法先は上下左右の`[3,5] / [2,6] / [4,6]`だけで、斜めなし。
- 移動予定位置: 最終予測でルークが`[4,6]`へ表示され、取り消しで元へ戻った。
- 敵HP: 盤面と開いた敵詳細の双方で表示。
- 火種の罠: TURN 02の飛びかかりは`[4,5]`で停止、罠3ダメージ、残り移動停止、罠消滅、非隣接のため攻撃不発。予測・詳細・ログ・実行後位置が一致。
- HP0非描画: 追跡獣撃破の最終予測で盤面から消え、HP0の通常駒やKOゴーストは残らなかった。後続行動は取消。
- 予測と実行、対象、速度順、帯電、災印、ログに新しい回帰は見つからなかった。

## 優先度別の観察事項

### P0

なし。

### P1

#### [P1] 700×900の6イベント時、手札カード上端が初期viewport外へ落ちる

- 再現: 700×900、ページ先頭、敵3イベントがあるターンで味方命令を3枚登録する。
- 実際: 戦場bottom `484`、実行パネルbottom `856.53`、COMMAND HANDパネルtop `871.53`に対し、実際の`#hand top=924.53`。画面内には見出しだけが残り、カードへ進むにはスクロールが必要。
- 期待: 6イベントでも手札カード上端が900px viewportと交差する。ACT 10実装記録のheadless値`899.14`は実機の日本語フォント折返しを再現していない。
- 影響: 敵の予定と6件の順番を読んだ直後、差し込むカード候補を同じ初期画面で認識できず、中央読路の完了条件を外す。
- 最小修正候補: 700px域でイベントカードの縦padding／gapまたは補助行高を約25px削減する、あるいはexecution panel下部の余白を詰める。効果文や対象情報を削らず、実機のYu Gothic UIで`#hand top < 900`を回帰条件にする。

#### [P1] 320×900でページ横overflowと手札の追加横スクロールが発生する

- 再現: 320×900へ変更し、ページ先頭と画面下部を確認する。
- 実際: `documentElement clientWidth=305 / scrollWidth=321`で16pxの横overflow。`body { min-width: 320px; }`が縦スクロールバー分を除いたlayout viewportより広く、`.game-shell right=312`、ヘルプbutton `right=320.91`まで張り出す。ページ下部に全体横スクロールバーが見える。
- 同時に`.hand`も`clientWidth=278 / scrollWidth=324 / overflow-x:auto`となり、ACTION ORDER以外の横スクロール領域になる。
- 影響: ページ全体と手札とACTION ORDERの三つの横方向操作が競合し、盤面→実行読路→手札という一方向の読路を崩す。右端ヘルプも横位置によって欠け得る。
- 最小修正候補: 420px以下で`body`の`min-width:320px`を解除し、`.game-shell`を利用可能なclient width内へ収める。手札は1列gridまたは幅100%カードへ切り替え、横overflowは`#action-timeline`だけに限定する。

### P2

なし。

## 修正後の重点回帰

1. Yu Gothic UIの実ブラウザ700×900、敵3＋味方3イベント、最長の効果文を含む状態で`#hand.getBoundingClientRect().top < 900`。
2. 320×900で`documentElement.scrollWidth === documentElement.clientWidth`、ヘルプbuttonのrightがclientWidth以下。
3. 320×900で横スクロール可能な要素は`#action-timeline`だけ。手札の全カード本文は省略せず縦方向に読める。
4. 700／320とも、ACTION ORDERの最初と最後へ手動スクロールおよびTabで到達し、フォーカスカード全体が見える。
5. 1280×720の戦場全体、18px主方向、6イベント、手札上端、横overflowなしを維持する。
6. ズーム操作可能な実ブラウザで200%へ変更し、文書横overflow、読路順、局所スクロール、フォーカス被覆を再確認する。

# ACT 10 corrective QA

更新: 2026-09-12 00:54 JST

## 判定

**PASS。ACT 10 QAで報告したP1二件は、実ブラウザの再測定で解消した。新規のP0／P1／P2不具合は確認されなかった。**

製品ファイル、`work/smoke-test.js`、DECIDE／ORIENTは編集せず、本節だけを追記した。

## 自動検査

- `node --check outputs/order-3/game.js`: PASS
- `node --check work/smoke-test.js`: PASS
- `node work/smoke-test.js`: PASS（`ORDER//3 smoke tests passed`）

## 実ブラウザ環境

- localhost: `http://127.0.0.1:8795/`
- キャッシュバスター: `?qa=act10-corrective-20260912-0035`、再接続後`?qa=act10-corrective-20260912-0045`
- ブラウザ: Codex in-app browser
- 計算済みフォント: `"Yu Gothic UI", "Hiragino Sans", system-ui, sans-serif`
- console warning／error: 0件

TURN 01〜03を通常操作で進め、TURN 03の生命吸収、息を整える、反撃姿勢を含む敵3イベントへ味方3命令を追加した。各イベントには番号、陣営、行動者、行動名、速度、対象、既存description由来の効果、`予測：`結果が残り、6枚すべてで縦内容の欠落はなかった。

## P1二件の修正確認

### 700×900 — PASS

- ページ先頭、敵3＋味方3の最長効果を含む6イベントで`#hand top=883.53`。前回の`924.53`から約41px上がり、`883.53 < 900`を満たした。
- 戦場`top=127 / bottom=484`、方向見出し`top=558`、ACTION ORDER`top=599.69 / bottom=771.53`、実行パネル`bottom=815.53`、手札`top=883.53`の順で、盤面→実行読路→手札が維持された。
- 方向見出しは18px。全6イベントの`scrollHeight=clientHeight=156`で、対象、効果節、予測を削除・クリップしていない。
- 文書は`clientWidth=685 / scrollWidth=685`で横overflowなし。
- ACTION ORDERは`clientWidth=647 / scrollWidth=982 / maxScrollLeft=335`。手動操作では`0→335`で最後、初期`0`で最初を完全表示できた。
- Tab順は1→6で実解決順どおり。1枚目は`scrollLeft=0`、6枚目は`335`となり、いずれもカード全体がコンテナ内かつviewport内に入った。

### 320×900 — PASS

- 文書は`documentElement clientWidth=305 / scrollWidth=305`、`body clientWidth=305 / scrollWidth=305`。前回の16pxページ横overflowは消えた。
- ヘルプbuttonは`right=297 <= clientWidth 305`で右端欠けなし。
- 6イベント時のACTION ORDERは`clientWidth=267 / scrollWidth=982`。実際に横スクロール可能な要素を全DOMから列挙しても`#action-timeline`だけだった。
- 手札は`display:grid`、1列`263px`、`clientWidth=263 / scrollWidth=263 / overflow-x:visible`。第二の横スクロールは消えた。
- 命令登録前の全5カードも実ブラウザで再確認した。各カードは`scrollWidth=clientWidth`かつ`scrollHeight=clientHeight`で、最長の連鎖火花を含む所有者、名称、効果全文、ALT移動、速度が縦に読めた。
- 戦場は`left=21 / right=284`で6×6全体が利用可能client width内に収まった。

## 1280×720回帰 — PASS

- 文書`clientWidth=1265 / scrollWidth=1265`、ACTION ORDER`clientWidth=723 / scrollWidth=723`で横overflowなし。
- 戦場全体は`top=137 / bottom=534.94`。
- 方向見出しは`top=102`、18px、weight 900。
- 6イベントはすべて`top=149.69 / bottom=319.53`かつ左右もviewport内。
- 手札上端は`648.94 < 720`。
- 敵詳細は初期`open=false`、`#intent-list display:none`を維持。

## 640×360（200%幅相当）— PASS

- 文書`clientWidth=625 / scrollWidth=625`で横overflowなし。
- ACTION ORDERだけが`clientWidth=587 / scrollWidth=982`で局所横スクロール。
- Tabで1枚目は`scrollLeft=0`、6枚目は最大`395`へ到達し、両端のカード全体が見えた。
- 盤面→実行読路→手札のDOM／視覚順を維持。

実ブラウザ200%ズームは再試行したが、Ctrl＋テンキー加算と`Ctrl+Shift+=`の双方でCSS viewport、visual viewport、DPRが変化しなかった。in-app browserはブラウザクロームのズーム操作を公開していないため、今回も640×360相当までを合格証跡とし、実200%そのものは未確認として残す。

## 軽回帰

- ACT 09 HP0非描画: 全スモークの死亡・途中／最終予測・解決・次ターン・終局回帰がPASS。実ブラウザでも盤面にHP0駒やKOゴーストは出ず、生存6駒だけを表示した。
- 敵説明: TURN 01の忍び寄る、庇護、災印を刻むと、TURN 03の生命吸収、息を整える、反撃姿勢の原文効果、対象、速度を維持。閉じたdetailsとの可視重複なし。
- 予測: 味方3命令の結界、装甲、移動、ダメージ、移動不能、帯電、連鎖結果を`予測：`で基礎効果と区別した。
- 順番: 6イベントのDOM／aria／Tab順が実解決順に一致。
- 320pxの1列化後もカード本文を省略していない。

## 優先度別結果

- P0: なし
- P1: なし（前回二件は解消）
- P2: なし

## 残留確認事項

ブラウザクロームを直接操作できる環境で、実ズーム200%だけは別途確認する。640×360の同等CSS幅ではoverflow、両端到達、フォーカス被覆の問題はなかった。

# ACT 11 QA

更新: 2026-09-12 01:56 JST

## 判定

**PASS。6状態の固定表示、予測／実状態、説明の開閉、タップと盤面操作の分離、HP0非描画、FE式隣接、Undo、1280／700／320px表示を確認し、P0／P1／P2不具合は見つからなかった。**

製品ファイル、`work/smoke-test.js`、DECIDE／ORIENT、Git、公開環境は変更せず、本節だけを追記した。公開版はACT 10のままである。

## 自動検査

- `node --check outputs/order-3/game.js`: PASS
- `node --check work/smoke-test.js`: PASS
- `node work/smoke-test.js`: PASS（`ORDER//3 smoke tests passed`）
- Nodeはbundled runtime `C:\Users\nonus\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe`を使用した。

## 実ブラウザ環境

- localhost: `http://127.0.0.1:8806/?qa=act11-20260912-0110`
- ブラウザ: Codex in-app browser
- TURN 01〜11を通常操作で進行し、味方・敵の付与、消費、持越し、ターン終了解除、撃破を確認した。
- console warning／error: 0件

## 6状態の表示と遷移

| 状態 | 実ブラウザで確認した内容 |
| --- | --- |
| 装甲 | TURN 01の庇護と割って入るで`盾4`／`盾2`、TURN 02の盾を固める後の災印吸収で`0から1`を表示。反撃姿勢の実状態では`盾6`かつ`.changed`なしとなり、通常装甲と持続装甲で正しい説明文へ切り替わった。 |
| 結界 | 無効印の最終予測でイオナに`結`、`結界`、`予測表示`を表示。災印起爆を無効化して消費され、次ターンに残らなかった。遺志：残響で付けた結界は盾の圧力の`露出`を無効化して消費され、露出を捏造しなかった。 |
| 移動不能 | 縫い留め直後に追跡獣へ`鎖`と`移動不能`を表示。飛びかかりは移動せず、ログも`縫い留められ、移動できない`となった。次ターンには解除されていた。 |
| 標的 | 移動不能と同時に`標`を固定位置へ表示し、次ターンへ持ち越した。次の縫い留めは狩人の印込みで4ダメージとなり、`標的-1`後に表示も消えた。 |
| 露出 | TURN 11の盾の圧力直後スナップショットで、ルークが`HP 8から1`、`露出`、`予測表示`となり、`露`チップは`shape-warning-triangle tone-rose changed`だった。直後の災印では露出込みの致死ダメージとなり、最終状態に表示残骸はなかった。 |
| 帯電 | 生命吸収で実際に2回復した追跡獣へ`⚡ 2`、`帯電 0から2`を表示。ターン終了後には消えた。連鎖火花の連鎖有無、0〜2の加算、消費、ターン終了消滅はスモークテストで補完した。 |

- 予測で新規・変化したチップには`.changed`が付き、実状態の`盾6`、`結`、`標`には付かなかった。実状態は比較元ではなく表示中スナップショットが状態の有無と値を決めていた。
- 0／falseの状態はチップ、結界外周、aria状態、説明参照を生成しなかった。数値状態は`盾12`、`帯電2`まで省略せず表示することを人工全状態テストで確認した。
- 人工的に1駒へ6状態を同時付与したスモークテストでは、`guard, ward, rooted, marked, exposed, charge`の順序、6個の形状クラス、3列×2行の非圧縮固定スロット、敵味方共通の描画経路、色以外の形状識別がPASSした。
- CSS検査では固定位置が上段`装甲／結界／移動不能`、下段`標的／露出／帯電`で、結界の二重輪とforced-colors時の輪郭も維持されていた。

## 説明・アクセシビリティ・入力分離

- 状態のない占有セルは`座標、陣営、名前、HP`だけ、状態のあるセルはその後へ固定順で状態、最後に予測表示を付加した。例: `5列 5行、敵、追跡獣、HP 8から7、最大8、移動不能、標的（狩人の印）、予測表示`。
- 状態があるセルだけが`aria-describedby`で同セル内の説明へ接続され、視覚ユニット subtree は`aria-hidden=true`だった。説明文は中央`statusMeta`と一致した。
- 盤面直後の説明領域はDOM上に1個だけで、閉時は`hidden`かつ空、開時は盤面の下・フッターの前へ通常フローで展開した。absolute／fixedの盤面被覆はない。
- 割って入るの対象選択中にイオナの状態面をタップしても、キューは`無効印`1件のまま、カードはselectedのまま、対象選択文も維持され、説明だけが`説明を固定中`として開いた。同じユニットの通常アイコンをタップすると割って入るが2件目へ正常登録された。
- 盤外タップとEscapeはいずれも固定説明だけを閉じた。空セルからTabを2回送って露出中のルークへ移動すると、セルがactiveになり、`有効な状態`の説明が開いた。
- 複数状態チップのホバー優先、pointer leave、タップ固定の状態非変更は人工全状態テストで補完した。

## 予測、実行、Undo、HP0

- TURN 01は味方3＋敵3の6イベントとなり、無効印、割って入る、3手目のヴェイル移動を実順へ挿入した。移動命令は移動予定位置`[1,5]`へ表示され、実行後位置と一致した。
- `一手戻す`で割って入るだけを除去すると、ルークとイオナの予測装甲が消え、無効印の結界だけが残った。再登録後も古い状態キャッシュは残らなかった。
- TURN 02は盾を固める→縫い留め→敵移動→狩人の印→災印起爆の順で、装甲吸収、結界消費、移動不能、標的の各スナップショットと実行ログが一致した。
- TURN 06の最終予測でイオナがHP0になると、イオナの占有セル、`.unit`、状態面、`#status-desc-iona`がすべて0件になった。実行後のTURN 07でも同じだった。TURN 09のヴェイル撃破予測でも占有セルが消え、実行後は盤面ユニット数が4へ減った。
- TURN 11では盾の圧力直後を選ぶと露出中の生存ルークへ可逆的に戻れ、最終予測では後続災印によるHP0後にルークと露出表示が消えた。

## FE式マスと軽回帰

- TURN 08、追跡獣が`[2,3]`、ルークが`[3,2]`で斜め隣接した状態では飛びかかりの攻撃が発生せず、ログは`届かなかった`となった。盾の圧力は同列上下隣接でのみ命中した。
- 移動候補は毎回上下左右だけだった。複数の移動命令では、1手目の予測移動先から2手目、2手目の予測移動先から3手目の合法セルを選べた。
- 敵味方混在ACTION ORDER、対象選択待ち、途中スナップショット、最終予測、敵HP、災印、庇護、反撃姿勢、生命吸収、死亡後の遺志カードに回帰は見つからなかった。

## レスポンシブ確認

| viewport | 結果 |
| --- | --- |
| 1280×720 | 移動不能＋標的、装甲を含む6イベント状態を目視。チップはHP表示の下の固定2段へ収まり、説明は盤面下、ACTION ORDERと手札は従来位置を維持した。ページ横overflowは見られなかった。 |
| 700×900 | 追跡獣の移動不能＋標的説明を固定して確認。説明は盤面とACTION ORDERの間へ展開し、盤面やカードを覆わなかった。ACTION ORDERだけが意図どおり局所横スクロールした。 |
| 320×900 | 33px級セルでも`盾1`、`鎖`、`標`を別スロットで識別でき、HPとの重なりや状態チップ同士の圧縮・重なりは見られなかった。手札は1列、ページ全体の横はみ出しは再発せず、ACTION ORDERだけが局所横スクロールを維持した。 |

全状態同時、ホバー優先、厳密な固定grid-area、closed時`display:none`、forced-colorsは通常操作で到達・切替しにくいため既存スモークテストで補完した。実ブラウザでは自然に到達した単独・複数状態、開閉、フォーカス、タップ、撃破、3 viewportを照合している。

## 優先度別結果

- P0: なし
- P1: なし
- P2: なし

## 残留リスク

6状態すべてを同じ駒へ同時に載せる局面、状態チップ単体のホバー優先、forced-colorsは人工テストのみである。通常プレイで到達した全6種類の個別状態と複数状態の組合せでは、人工テストとの差異は確認されなかった。

## 司令塔の受領・公開・次工程

- 2026-09-12 JST、QA担当からの直接報告 `act-11-qa-final` を受領し、上記PASSを受理した。
- 司令塔でも変更範囲を照合し、JavaScript構文検査と全スモーク検査の合格を確認した。
- 公開コミット: `ad9bd974ca2e3eaf14b266dc2b6034da3e530d24`。Pages実行 `34625992115` はsuccess。
- 公開先: https://usouso.github.io/order-3/ 。ACT 11の状態表示を公開済み。
- ユーザーの指示で定期監督を完全削除し、全担当からの直接完了報告へ切り替えた。3担当の新経路からの報告を受信済み。
- 次の未解決要望はカード用途の分類表示。設計担当へACT 12を依頼し、`act-12-design-01` の直接報告で次工程へ進める。
